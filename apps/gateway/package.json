{
  "name": "@toktracker/gateway",
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --minify --target bun && bun build bin/toktracker-gateway.ts --outfile dist/cli.js --target bun && bun ../../scripts/copy-dashboard.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@hono/node-server": "latest",
    "@toktracker/cli": "workspace:*",
    "@toktracker/shared": "workspace:*",
    "@toktracker/token-calc": "workspace:*",
    "hono": "latest"
  }
}
